'use strict';

describe('myApp.coding module', function() {

  beforeEach(module('myApp.coding'));

  describe('coding controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var CodeCtrl = $controller('CodeCtrl');
      expect(CodeCtrl).toBeDefined();
    }));

  });
});
